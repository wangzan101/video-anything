[pytest]
pythonpath = .
markers =
    current_behavior: characterization coverage for current fetch.sh behavior
    phase1_capability: capability and runtime contract ownership
    phase2_core: fetch core contract ownership
    phase3_publish: publish, reuse, lock, and recovery contract ownership
